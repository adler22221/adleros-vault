# System 0 Audit — adlerOS Orchestrator spec, re-examined

- **File under audit:** `C:\Users\adler-standard\Documents\_AI agents\global-audit\05_blueprint\adleros-orchestrator.md` (a second, diverging copy lives at `C:\Users\adler-standard\Documents\Obsidian\2ndbrain\08_global-audit\adleros-orchestrator.md`)
- **Audit date:** 2026-07-07
- **Method:** four independent lenses — fidelity to your own design docs (L1), fidelity to your endorsed references (L2), technical soundness (L3), adoption/stall risk (L4) — each adversarially re-verified against the live filesystem and your member-check answers before anything entered this report.
- **Ground truth of intent:** `P:\adlerOS\ops\vault-map.md`, `P:\adlerOS\ops\conventions.md`, `P:\adlerOS\ops\skill-map.md`, `P:\adlerOS\ops\me.md`, your questionnaire verdicts (`C:\Users\adler-standard\Documents\Obsidian\2ndbrain\08_global-audit\member-check-questionnaire-v2.md`), and the live vault itself. The spec is the thing under audit, not a source.

---

## 1. Verdict in one paragraph

Roughly half of the spec survives, and it is the half that restates what you already designed. The parts that check out — vault lanes as the shared coordination surface, the entropy firewall with human-only promotion, the capture → triage → draft → promote loop, the model router, the "grow it, don't build it" rollout, and the ~1 hr/week capacity guardrail — all trace line-for-line to `P:\adlerOS\ops\vault-map.md` and `conventions.md`, and the underlying coordination shape (a "blackboard": one shared workspace everything reads and writes, instead of systems messaging each other) is technically the right choice for one human plus a handful of occasional agents. What does not survive: the spec's two load-bearing factual claims are both false. The 15-lane migration it calls "staged but not executed" in fact ran on 2026-06-24/25 (your new journal/tasks/projects/ideas lanes hold ~2,000 files today), and the hook-based enforcement it describes as existing does not exist anywhere on either machine. Its headline recommendation — run "unstall Stage B" as a standing parallel foundation track — is built on those stale facts, contradicts the spec's own "not a big-bang build" section, and reproduces the exact undeadlined-infrastructure pattern your own record shows stalling; its two live copies even contradict each other on this very point. Its most striking idea, "emergent cross-system flows," quietly violates the firewall rule it inherits from you. And it makes one real privacy error: it lists `12_people` as freely AI-readable canon when your design marks it sensitive. None of this requires abandoning the architecture. It requires re-grounding the spec in the vault as it exists today, cutting four premature infrastructure ambitions, and adding about six missing pieces — most of them one-paragraph design decisions, not builds.

---

## 2. Q1 — Does the blackboard/stigmergic model match your intent?

**Yes at the skeleton. No at three joints. And the vocabulary is the spec's, not yours.**

**Where it genuinely matches (grounded in your own files):**
- The core mechanism — shared lanes as coordination state, AI writes quarantined in `06_agent-out`/`07_agent-synthesis`, human-only promotion to canon — is a direct restatement of your own design: `P:\adlerOS\ops\vault-map.md` lines 6-32 (the lane table and the entropy-firewall rule) and `P:\adlerOS\ops\conventions.md` lines 39-43 (the status machine). The spec's L38 mapping table is largely accurate because it copies you.
- The intent behind it also checks against your member-check answers: you affirmed deliberately building external, prosthetic structure to offload self-regulation (clm-0034, `member-check-questionnaire-v2.md` L112) and denied any identity-need to personally be the hub (clm-0036, L76) — supporting a structural coordination layer rather than "Adler routes everything."
- "Blackboard, not switchboard" is a correct technical name for what you built. "Stigmergic" (coordination by leaving traces in a shared medium, the way ants follow trails, rather than by direct messages) is a fair label for the same thing.

**Where it does not match:**
1. **The words and the nine-system carve-up are not yours.** Nothing you wrote uses blackboard/stigmergy, and the nine "capacital systems" decomposition exists only in AI-authored documents; no member-check claim ratifies it (spec L8-9, L38 "the mapping is exact" overclaims). It should be presented as *the audit's name for your design*, pending your sign-off.
2. **The spec hardens your case-by-case gate into permanent law.** Your verdicts on the agent-boundary claims are "Nuance — depends on use-case" (clm-0027, L105) and "provisional, quality-driven, expected to relax" (clm-0039, L111). The spec cites clm-0027 as warrant for a uniform, permanent, categorical firewall (spec L15, L27). Your own `deleg:` T0–T4 field (`conventions.md` L45-47) is the graduation mechanism the spec should have used.
3. **"Emergent cross-system flows" (spec L18-22) contradict the firewall they sit next to.** Under your rule, one system's AI output cannot be read by another system until you promote it — so the flows are either rule-breaking (AI reading AI) or throttled to your personal promotion rate, which is the bottleneck the spec says it exists to remove (L6). This needs a design decision from you, not silence (see tweak C5).
4. **It claims to BE your everyday note-taking system (L37) while omitting your thinking layer entirely** — no wikilinks, no MOCs ("maps of content," your own convention: "MOCs on demand at squeeze points," `conventions.md` L57), no idea-maturity pipeline (`04_ideas`, `vault-map.md` L10) that feeds your book. As a coordination/governance spec it matches you; as "your note-taking system" it does not yet.

---

## 3. Component-by-component table

Severity: **CRIT** = false fact or safety/privacy hole; **MAJ** = will cause failure or abandonment if unfixed; **min** = accuracy/clarity fix.

### Keep (7)

| # | Component (spec location) | Verdict | Sev | Evidence |
|---|---|---|---|---|
| K1 | Blackboard coordination model — shared vault state, no system-to-system calls (L8-9) | KEEP | min | Right model for 1 human + low-frequency agents; structurally identical to `vault-map.md` L6-32 (L1-9, L3 lens) |
| K2 | Entropy firewall as design principle — quarantined AI lanes, human-only promotion (L14-16, L27) | KEEP | — | Authentically yours: `vault-map.md` L29-32, `conventions.md` L39-43 (L1 lens, L2-8) |
| K3 | Governance loop: capture → agent triage → draft to `06` → human review/promote (L11-16); GTD rhythm preserved, no PARA smuggled in | KEEP | — | Near-verbatim from the governance model you solicited and adopted: `metaref1_perplexity_researchworkflowneeds.md` §3.4 L470-483 (L2-8) |
| K4 | Lane mapping table (L40-49) — except the canon row (see C2) | KEEP | — | Copies `vault-map.md` accurately otherwise (L1 lens) |
| K5 | Model router as standing cost policy (L13, L26, L32) | KEEP | — | Exists, inherited; matches `conventions.md` L49-51 (L4-1) |
| K6 | "It is NOT a big-bang build" section (L29-35) | KEEP as the normative core | — | Matches your stall diagnostic exactly: `gap-analysis.md` §2; lanes live today (L4-1) |
| K7 | Capacity-paradox guardrail — ~1 hr/week review cap, stop-and-shrink rule (L55-56) | KEEP as binding constraint | — | Aligned with near-zero slack (clm-0018, questionnaire L65) (L1-10, L4-1) |

### Change (15)

| # | Component (spec location) | Change to | Sev | Evidence |
|---|---|---|---|---|
| C1 | "The mapping is exact" + nine-system decomposition presented as yours (L8-9, L38) | Relabel "our name for your design"; you ratify (or re-carve) the 9-system split | min | Vocabulary/decomposition exist only in AI-authored docs (L1-9) |
| C2 | Canon row lists `12_people` as AI-readable canon; omits `00/02/03/04` (L47) | Copy `vault-map.md`'s firewall column exactly: canon = 00,01,02,03,04,10,11,80; `12_people` = sensitive, flag-gated | MAJ | `vault-map.md` L16, `conventions.md` L30-33 (L1-5, L2-3) |
| C3 | "Enforced by PreToolUse hooks + write-allowlists" stated as existing (L27, L49) | Mark UNBUILT; scope as first build item: folder-path deny rules covering both path roots; state honestly what hooks can and cannot enforce | CRIT | `C:\Users\adler-standard\.claude\settings.json` has no PreToolUse hook anywhere (L1-2, L3-2) |
| C4 | Uniform, permanent promotion gate (L15, L27) | Gate strictness scales with stakes via your own `deleg:`/`sensitive:` fields; hard-gate stays on voice/relational/T3/money/authorship | MAJ | clm-0027 "Nuance: depends on use-case" (questionnaire L105), clm-0039 L111; `conventions.md` L45-47 (L1-4, L4-3) |
| C5 | "Emergent cross-system flows" (L18-22) | Make the promotion dependency explicit; optionally define a narrow class of machine-readable operational signals agents may read | MAJ | Flows as written violate `vault-map.md` L29-32 or throttle to your promotion rate (L1-3, L2-2) |
| C6 | "Never read back as source" rule taken literally (L46; `vault-map.md` L13) | Two-tier rule: agents MAY read AI lanes as working state (track/dedupe/summarize); may NOT import claims into canon-bound drafts without `[VERIFY:]` tags | MAJ | The design's own tracker/digest loops violate the literal rule (L3-4); mechanism already in `conventions.md` L43 |
| C7 | Dispatch queue written to `07_agent-synthesis` (L33, L16) | Queue in `06_agent-out` (or an ops-side file); `07` stays regenerable digests only | min | `vault-map.md` L12-13, `skill-map.md` L26-27 (L2-4) |
| C8 | "STALLED at Stage B... staged but not executed" + "a stalled orchestrator is why systems feel uncoordinated" (L51) | Replace with the verified state: migration ran 2026-06-24/25; open items are enforcement, `12_people` split, `10/11` lanes, media archival, cleanup. Re-derive the diagnosis: the gap is enforcement + zero live systems, not lane structure | CRIT | Live vault counts: `01_journal` 871, `02_tasks` 1052, `03_projects` 28, `04_ideas` 40 files, populated 2026-06-24/25 (L1-1, L4-4) |
| C9 | Daily scheduled meta-agent (L26, L33) | Weekly or on-demand, aligned to the sitting; keep the ≥2-live-systems trigger (L35) as hard precondition; validate the Cubi model on ~10 real inbox items first; add a dead-man heartbeat (digest written every cycle even when empty, so a missing file is itself the alarm) | MAJ | DSPS/no-fixed-cadence (clm-0024, questionnaire L95); silent-failure risk; Qwythos benchmark is speed-only (`HANDOFF.md` L13) (L1-8, L3-9, L4-2, L4-7) |
| C10 | "T3 → Cubi-local" routing rule (L13, L25) | Rename privacy tiers P0–P3 (or always write "privacy-T3"); restate routing so it composes with your token-order routing | MAJ | Your T3 = delegation tier "AI executes, I approve — DEFAULT" (`conventions.md` L45-47); collision would misroute nearly all default work (L1-7) |
| C11 | Privacy tier assigned at dispatch, by reading the item (L13, L26) | Tier at capture: a separate T3 capture channel landing directly on the Cubi; default cloud-visible inbox declared T1/T2-only; untagged items triaged local-first | MAJ | A cloud router reading a sensitive capture to classify it has already leaked it — the architecture's own named worst failure (`capacital-blueprint.md` §6) (L3-6) |
| C12 | "T3... outside every sync path" with no backup story (L25) | Add a hard requirement: scheduled encrypted local backup (encrypted archive to external disk or as an encrypted blob to pCloud — encrypted-at-rest never transits a cloud model) | CRIT | Only data class in the corpus with zero backup; a Cubi disk failure is total loss (L3-3; contrast `migration-runbook.md` L3-4, 67-68) |
| C13 | Two diverging live copies of the spec | Pick ONE canonical file; the other becomes a pointer | MAJ | Blueprint copy L53 says parallel foundational track; in-vault copy L43 says "not prerequisites... Build ON, never wait" (L1-6) |
| C14 | Lane-count inconsistency across audit docs (spec says 15; `gap-analysis.md`/`capacital-blueprint.md` say 13) | Normalize everything to `vault-map.md`'s 15 lanes; add a line declaring `vault-map.md` the authoritative source future edits diff against | min | `vault-map.md` table = 15 lanes; the stale "13" lives in the earlier audit docs, and in the in-vault spec copy L43 (L4-8, L1-6) |
| C15 | "Weekly sitting" as the promotion point (L16) | Event-driven/instance-attached review — the digest surfaces when you open the relevant project hub or `_Home.md` — with the ~1 hr/week cap as the binding constraint | min | Your reflection is instance-based, not periodic (clm-0031, L109); periodic reviews are your documented lapse point (`skill-map.md` L19); `07` has 0 files ever (L1-10) |

### Drop (3)

| # | Component (spec location) | Replace with | Sev | Evidence |
|---|---|---|---|---|
| D1 | "Unstall Stage B" as a foundational parallel track (L51-53) | Pull-based migration: a lane is touched only when a live system needs it, as a ≤30-min batch inside that system's deadlined sitting; the bulk remainder declared non-blocking | CRIT | Contradicts spec's own L31; nothing waits on it (`gap-analysis.md` L68); a standing undeadlined track is the documented failure configuration (`capacital-blueprint.md` L168) (L3-8, L4-4, L4-5, L4-6) |
| D2 | Event-hook dispatch — "a new email/note/gnucash-export fires the right system" (L26) | Scheduled polling (cron — a scheduler that runs a script at fixed times — reads the inbox and processes anything new); real PreToolUse hooks reserved for the firewall deny-list only | MAJ | Claude Code hooks fire on tool calls, not on files/emails arriving; this re-specifies the never-started capture daemon (`gap-analysis.md` row 4) (L3-7, L4-2) |
| D3 | MVP extras: Cubi local T3 lane-analog before any T3 system ships (L25); AI-wiki; cross-system load-governor wiring (L22); voice calibration in the first pilot | Defer all to named triggers (first T3 system live; month-6 review); load-governor becomes one line in the dispatch skill prompt reading `00_horizons` | MAJ | Each duplicates something cheaper; platform-before-it-pays profile (L3-10, L4-2) |

### Add — missing from the spec (6)

| # | Missing piece | Sev | Evidence |
|---|---|---|---|
| A1 | Cubi write path + single-writer rules: which device may write which lane (per-device subfolders); phone reads digests via SSH-to-Cubi, not via vault sync; vault sync treated as eventual archival, never the delivery mechanism | CRIT | Vault is double-synced (Obsidian Sync + pCloud mirror, `migration-runbook.md` L3); your own runbook pauses both and closes Obsidian before batch writes (L8-12, 62); the spec never says how the Cubi reaches the vault (L3-1) |
| A2 | Item-claim protocol: claim-by-move (processing an inbox item moves it to `processed/` — an atomic rename acts as the lock); one consumer per lane in the MVP; per-writer tracker files merged at digest time | MAJ | Three dispatch triggers can process the same item; concurrent tracker writes = silent lost updates across two sync layers (L3-5) |
| A3 | Degraded-mode governance: a manual promotion checklist + a scripted read-only scan of `author:`/`status:`/lane placement, in force until hooks are actually built | MAJ | Today the firewall runs "by memory" — the thing `vault-map.md` L32 forbids; your adopted reference requires a manual fallback for every automated step (`metaref1` L153-158) (L2-7) |
| A4 | Thinking-layer section: within-canon organization is links/MOCs/Dataview per `conventions.md` L56-57; every agent note carries wikilinks; `04_ideas` maturity pipeline (seed → to-publish) named as the idea-to-book flow the orchestrator serves | MAJ | The spec claims to be your note-taking system with zero of your linking layer; your #1 Mission output runs through `04_ideas` (`vault-map.md` L10, `me.md` L46) (L2-1) |
| A5 | One clarifying line: "lanes encode provenance and write-permission only; knowledge structure lives in links/MOCs" | min | Prevents future builders mistaking lanes for topic taxonomy; consistent with LYT and `vault-map.md` L37 (L2-6) |
| A6 | Crystal discipline for orchestrator artifacts: routing policy as a frozen human-approved table; digests carry a based-on/staleness flag; regeneration is a deliberate act | min | Your own anti-rag architecture argues against implicit re-synthesis (`anti-rag.md` L7-19, L100-104) (L2-5) |

**Tally: 7 KEEP · 15 CHANGE · 3 DROP · 6 ADD.**

---

## 4. The tweaks, explained

Grouped by theme. Each entry: what the spec says → what the evidence says → the concrete revision.

### 4.1 The status story is stale, and the roadmap advice built on it points at the wrong problem (C8, D1, C13, C14)

**Spec:** L51 — the orchestrator "is STALLED at Stage B"; the 15-lane reorg is "staged but not executed"; and "a stalled orchestrator is *why* the nine systems currently feel uncoordinated." L53 — therefore run "unstall adlerOS Stage B" as a "foundational track that runs in parallel."

**Evidence:** The migration ran. Independent filesystem check of `C:\Users\adler-standard\Documents\Obsidian\2ndbrain` on 2026-07-07: `01_journal` 871 files, `02_tasks` 1052, `03_projects` 28, `04_ideas` 40 — all populated 2026-06-24/25, two days after the staging files the spec cites. The old source folders (`009_tasks`, `001_dailynotes`) are gone. What remains open is real but different: `12_people` holds 1 note while `801_Reference_Persons` still holds 155; `07_agent-synthesis` has never held a file; `90_media` has 5 files against ~2,570 unarchived attachments; `10_knowledge` and `11_frameworks` were never created; and no enforcement was ever built. So the honest status is *partially executed*, not "not executed" — and the systems feel uncoordinated because zero of the nine systems exist yet (`gap-analysis.md` §2: five needs unserved, CN-01 with zero artifacts), not because lane folders have the wrong names.

The "parallel foundational track" advice is also the shape of work your record says quietly starves: undeadlined infrastructure with no live consumer (clm-0035 — your verdict: deliberate deprioritization of dream-infra under bread pressure; clm-0010; clm-0018 — near-zero slack). `capacital-blueprint.md` L168 states it flatly: this is exactly how the 13-lane vault and the capture daemon died the first time. And the spec's own L31 already says the lanes work today — L53 contradicts it. Finally, the two live copies of this spec give opposite advice on this exact point (blueprint copy L53: parallel track; in-vault copy L43: "not prerequisites... Build ON, never wait"), and the in-vault copy carries a stale "13-lane" count where `vault-map.md` has 15.

**Revision:** Rewrite L51-53. (1) State the verified partial-execution status. (2) Delete the causal claim; the gap is *enforcement + no live systems*. (3) Replace the parallel track with pull-based migration: a lane gets touched only when a system you're shipping needs it, as a ≤30-minute batch inside that system's own deadlined sitting; declare the bulk remainder non-blocking so its non-completion stops registering as failure. (4) Keep exactly one canonical copy of the spec. (5) Normalize every audit document to `vault-map.md`'s 15 lanes and name `vault-map.md` as the authoritative source. Note: your stall pattern here is treated as a design input — the system should route around it, not ask you to be different.

### 4.2 The firewall: right rule, wrong status, too rigid, one privacy error (C3, A3, C4, C2, C6, C7)

**C3 — enforcement is claimed, not built.** Spec L27/L49 say the firewall is "enforced by PreToolUse hooks + write-allowlists (not memory)... not as policy hope." (A PreToolUse hook is a check that runs before an AI tool call executes and can block it; a write-allowlist is a fixed list of folders an agent may write into.) Verified: `C:\Users\adler-standard\.claude\settings.json` contains no PreToolUse hook, and no project-level settings exist at the vault, `P:\adlerOS\ops\`, or the audit workspace. The wording is copied from your own `vault-map.md` L32 — faithful to your *intent* — but stated as fact it is false, and today the firewall runs on instructions-in-memory, which is precisely what your rule forbids. Revision: mark it UNBUILT and scope it as the first genuine build item — folder-path deny rules covering *both* vault path roots (the vault is reachable as `C:\...\Obsidian\2ndbrain` and as the pCloud mirror `P:\Obsidian\2ndbrain`, `migration-runbook.md` L3), version-controlled once in `ops\` and shared to both machines. Be honest about scope: path-based rules make the *write side* structural inside Claude Code; the read-side quarantine, raw shell writes, and anything outside Claude Code (Ollama/Open WebUI on the Cubi, Obsidian plugins) remain convention plus review (L3-2). Promotion should be *moving the file out of `06_agent-out`* — a folder boundary a rule can see — rather than editing a frontmatter field (the YAML metadata block at the top of a note), which no path rule can police.

**A3 — until then, a named manual fallback.** Your adopted reference explicitly requires "a clear manual fallback for every automated step" (`metaref1_perplexity_researchworkflowneeds.md` L153-158). Revision: a short human-run promotion checklist plus a read-only script that scans `author:`/`status:` fields and lane placement for violations, run at the sitting. Minutes of work; closes the "policy hope" window.

**C4 — the gate is yours; its uniformity is not.** Spec L15/L27 cite clm-0027 as a settled blanket rule ("never send/delete/advance-canon/touch-T3/speak-in-your-voice") inside a permanent layer. Your actual verdict on clm-0027 is "Nuance: depends on use-case" (questionnaire L105), and on clm-0039 you called the human-only gate strict where institutionally required (authorship, the book) and elsewhere provisional, expected to relax as AI quality improves (L111). You already built the graduation dial: `deleg:` T0–T4 per note (`conventions.md` L45-47). Revision: promotion strictness routes through `deleg:`/`sensitive:` per note. Hard gate permanently on: anything in your voice, anything relational/sensitive, money, canon advancement, and the book. Trust-band/spot-check path from day one for low-stakes structured output (tracker rows, digest regeneration, reference notes). Mark the strict default as reviewable.

**C2 — the one privacy error.** Spec L47 lists `12_people` in "Canon (the only AI-readable source)." Your design marks it **sensitive**, not canon (`vault-map.md` L16), with `crm:`/`sensitive:` flags gating what agents may do (`conventions.md` L30-33: `sensitive: true` is excluded from digests entirely). The row also omits `00_horizons`, `02_tasks`, `03_projects`, `04_ideas`, which *are* canon — and which the spec's own dispatch layer reads (L12-13, L25), so the spec contradicts itself. Revision: reproduce `vault-map.md`'s firewall column exactly.

**C6 — the quarantine rule, read literally, forbids the design's own loops.** "AI output is never read back as a source" (`vault-map.md` L29-32) is aimed at a real disease — AI re-ingesting AI prose until errors compound. But the spec's own tracker and digest loops require agents to re-read files in AI lanes as *working state* (the opportunity tracker in `06_agent-out`, `capacital-blueprint.md` §4; the status-digest reading the tracker, `operations-coordination.md` L79). Revision: split the rule in two. Agents MAY read AI-lane files as *objects/state* — to track, dedupe, summarize, verify. Agents may NOT import *claims* from AI-lane content into canon-bound drafts unless tagged `[VERIFY: basis]` — the mechanism your `conventions.md` L43 already defines. Acknowledge the read side is convention, not hook-enforceable.

**C7 — small lane-semantics fix.** Spec L33 has the dispatch agent write its work queue to `07_agent-synthesis`; your design defines `07` as quarantined, regenerable, never fed back (`vault-map.md` L13) — but a queue must be re-read to act on it. Your design already has the correct lane: `06_agent-out`, "proposals awaiting your promotion (transient)" (L12). Revision: queue in `06`; `07` stays digests-only.

### 4.3 Cross-system flows: the spec's flagship idea needs one honest sentence and one design decision (C5)

**Spec:** L18-22 — "Cross-system flows are emergent, through the vault": a Finances figure read by the Relationship system for [partner]-support planning; a Research finding picked up by Outreach; Health load limits honored by dispatch everywhere.

**Evidence:** Under your firewall, System A's output sits in `06_agent-out` until *you* promote it; only then may System B read it. So as written, the flows either break your rule (AI reading AI) or wait on you — meaning cross-system throughput equals your promotion rate, which is the exact "everything bottlenecks on one nervous system" problem the spec opens with (L6). The spec never mentions this. No Adler-authored document describes AI-to-AI cross-system flows; this section is invention (L1-3, L2-2).

**Revision:** (1) Add the honest sentence: cross-system flows are human-clocked — their latency is your review cadence — and estimate the weekly promotion load. (2) Then decide, with you, whether to open a narrow, typed exception: a small set of machine-readable *operational signals* (a runway number, a load limit, a task status — structured frontmatter fields, not prose) that agents may read from approved output without per-item review. That distinction — reading operational signals vs. ingesting AI prose as a source — preserves what the firewall actually protects against. This is question 4 in §6; it is your call, not the spec's.

### 4.4 Dispatch: keep the skill, demote the daemon, fix the T3 collision (C9, D2, C10, C11, A2)

**C9 — the daily meta-agent.** Spec L26/L33 add a daily scheduled meta-agent as a dispatch pillar. Evidence: your documented sleep-phase pattern and health-based schedule governor argue against any fixed daily human cadence (clm-0024, questionnaire L95; clm-0023 L94); a daily digest with a weekly reader accumulates as guilt-weight; everything scheduled funnels through one always-on machine that fails *silent* by design (pull-only, no push alerts); and the Cubi's local model has only ever been benchmarked for speed, never for whether it can produce reliable structured routing output (`HANDOFF.md` L13). Revision: the meta-agent runs weekly (aligned to the sitting) or on demand; the spec's own trigger — stand it up only when two systems are live (L35) — becomes a hard precondition; before any schedule depends on the local model, validate it on ~10 real inbox items; and add a dead-man heartbeat — the digest file is written every cycle even when empty, dated, so a *missing* file is itself the alarm at the next sitting.

**D2 — event hooks aren't a thing you can build this way.** Spec L26: "hooks (event-triggered — a new email/note/gnucash-export fires the right system)." Claude Code hooks fire around tool calls inside a running session; nothing watches your filesystem or inbox. Making this real means a standing watcher daemon — which is the capture daemon, the corpus's canonical never-started project (`gap-analysis.md` row 4: no script, config, or log anywhere). Revision: delete event-triggered dispatch; scheduled polling gives identical user-visible behavior at your volume with zero new daemon. Reserve real hooks for the firewall deny-list.

**C10 — "T3" means two opposite things.** In your `conventions.md` L45-47, T3 is a *delegation* tier: "review (AI executes, I approve — DEFAULT for new automation)." The spec's "T3 → Cubi-local" (L13, L25) uses the audit workspace's *privacy* T3 (most sensitive). Read with your meanings, the rule would route nearly all default work to slow local models, contradicting your own routing order (`conventions.md` L49-51). Revision: rename privacy tiers P0–P3 everywhere (or always write "privacy-T3"), and restate routing so it composes with your token-order routing and budget.

**C11 — classify sensitivity at capture, not at dispatch.** Spec L13/L26 has the router *read* an inbox item to assign its privacy tier. But if a sensitive family-related capture is read by a cloud model to decide it's sensitive, the leak has already happened — the architecture's own named worst failure (`capacital-blueprint.md` §6). Revision: tier at capture — a separate sensitive-capture channel landing directly on the Cubi (distinct Telegram bot or keyword prefix, or direct note over SSH), the default cloud-visible inbox declared P1/P2-only, and untagged items triaged local-first before anything reaches a cloud prompt.

**A2 — two cheap coordination primitives the blackboard needs.** Nothing marks an inbox item as taken, so the cron run, a manual skill, and (if built) any event trigger could all process the same capture; and two writers updating one tracker file across two sync layers silently lose updates (the exact hazard your runbook's single-writer note names, `migration-runbook.md` L62). Revision: *claim-by-move* — processing a capture moves it into `05_collab-inbox\processed\` (a filesystem rename is atomic: it either fully happens or doesn't, so it works as a lock); one consumer per lane in the MVP; per-writer tracker files (e.g. `tracker-cubi.md`) merged at digest time.

### 4.5 Devices and durability: the two silent data hazards (A1, C12)

**A1 — the Cubi write path is unspecified over a double-synced vault.** The vault syncs two ways at once: Obsidian Sync (which only moves files while the Obsidian app is running) and a pCloud mirror (`migration-runbook.md` L3; memory note `obsidian-sync-requires-app-running.md`). The spec has the Cubi writing digests and the phone approving them, but never says how the Cubi reaches the vault; the implied chain silently requires your laptop awake with Obsidian open, and your own runbook treats concurrent sync as dangerous enough to pause everything and go single-writer before batch writes (L8-12, L62). Revision: one write channel per device — the Cubi writes only under a Cubi-owned subfolder no other device writes; the phone reads digests and approves over SSH to the Cubi (a path the blueprint already provides), not through the vault; vault sync is archival, never the delivery mechanism.

**C12 — the most sensitive data is the only data with no backup.** Spec L25: sensitive systems live in a "local-only analog on the Cubi, outside every sync path." Deliberate — but as specified, the hardest-to-reconstruct artifacts (financial runway models, family-related chronologies and briefing material) live on one consumer mini-PC's disk with zero copies; every other data class in your corpus has three (`migration-runbook.md` L3-4, 67-68). Revision: one cron line — nightly encrypted archive (e.g. 7z-AES or age) to an external disk, or as an encrypted blob to pCloud; encrypted-at-rest never transits a cloud model, so the privacy rule holds. Write it into the spec as a hard requirement.

### 4.6 The human loop: make review land where your attention already goes (C15)

**Spec:** L16 — you read the `07` digest "at the weekly sitting." Evidence: `07_agent-synthesis` has never contained a file; your own `skill-map.md` L19 describes `/reflect` as reviving "the lapsed reviews" — periodic review is your documented lapse point; and your member-check says your reflection is instance-based (attached to projects), not calendar-based (clm-0031, L109). A standing weekly obligation is the known failure shape. Revision: promotion review is event-driven and instance-attached — the pending-promotions digest surfaces when you open the relevant project hub or `_Home.md` (which already carries guardrail blocks) — with the ~1 hr/week cap (K7) as the binding constraint rather than a fixed appointment.

### 4.7 The missing half of "your everyday note-taking system" (A4, A5, A6)

**A4 — the thinking layer.** Spec L37 claims System 0 *is* your everyday note-taking + project-management system, yet contains no wikilinks, no MOCs, no block-level linking, no graph/canvas, and no `04_ideas` row — the layer your self-authored references actually center on: your only self-named method is Zettelkasten with section-level linking (`metaref1` L13-16), your conventions mandate wikilinks on every agent note and "links carry structure... MOCs on demand at squeeze points" (`conventions.md` L56-57), and `04_ideas`' maturity pipeline (seed → to-publish, `vault-map.md` L10) feeds the book that is your #1 Mission output (`me.md` L46). Revision: either retitle the section to claim only the coordination role, or add a thinking-layer section stating that within-canon organization is links/MOCs/Dataview, agent notes must carry wikilinks, and the `04_ideas` pipeline is the flow the orchestrator ultimately serves.

**A5 — one sentence that prevents a future mistake.** The 15 lanes do *not* violate your links-over-folders philosophy, because they partition by who-may-write and provenance, not by topic (`vault-map.md` L37 "split by role, not by folder") — but the spec never says so, and a future builder could mistake lanes for a knowledge taxonomy. Add: "lanes encode provenance and write-permission only; knowledge structure lives in links/MOCs."

**A6 — apply your own anti-rag discipline to the orchestrator's artifacts.** Your research-hub architecture argues against implicit query-time re-synthesis (variance across runs, full cost every query) and for synthesize-once/freeze/flag-staleness/regenerate-deliberately (`anti-rag.md` L7-19, L100-104). The spec's digests and routing decisions have no frozen form or staleness flags. Add: routing policy is a frozen human-approved table; every digest carries a based-on/staleness field; regeneration is a deliberate act.

---

## 5. Q2 — Roadmap sequencing

**Recommendation: modified option (b) — scorer START-HERE now; Stage B dissolved into pull-based micro-batches, not run as a parallel track.**

The two options on the table were (a) lead with unstalling Stage B, or (b) start the opportunity-fit-scorer with Stage B as a parallel foundational track. Lens L4's analysis, which this report adopts:

- **Against (a):** Stage B mostly already ran (§4.1), and what remains is undeadlined infrastructure with no live consumer — the task profile with the worst completion base rate in your own record (clm-0035, clm-0010; near-zero slack per clm-0018). Leading with it stakes the roadmap's credibility on the one bet the evidence says loses.
- **Against (b) as literally written:** a "parallel foundational track" is a standing undeadlined project running beside bread work — `capacital-blueprint.md` L168 names this as exactly how the 13-lane vault and the capture daemon died. The staged files have already been aging since 2026-06-23; a parallel track converts to guilt-weight, not execution.
- **For the scorer half of (b):** it extends your one proven daily delegation habit, needs no new infrastructure, trust, or privacy exposure, is a one-sitting build with a built-in validation step (`capacital-blueprint.md` §4, L105-119), and produces the rubric later systems reuse. Best-evidenced first move in the corpus. Nothing downstream waits on Stage B (`gap-analysis.md` L68).

**The first week under this plan:**

1. **Sitting 1 (one afternoon, laptop).** Build the opportunity-fit-scorer exactly per `capacital-blueprint.md` §4 steps 1-4, logging rows in the *live* `06_agent-out` lane of 2ndbrain. No waiting on anything.
2. **Same sitting, +30 min — the first pull-based Stage-B micro-batch.** Apply the already-written frontmatter schema (`status:`/`author:` axes from `P:\adlerOS\ops\vault-staging\README.md` + `conventions.md`) to the scorer's tracker and templates. One real piece of Stage B goes live with zero note moves.
3. **15-minute validation.** Score 2-3 live opportunities; compare against your gut ranking. If it disagrees in ways that feel wrong, shrink or stop — that is the K7 guardrail working, not failure.
4. **Write a one-line decision note:** "Stage B = per-system pull migration; no standalone migration sittings; meta-agent, hooks, and Cubi-analog deferred until their named triggers." This converts three open loops into closed decisions.
5. **10 minutes:** pick the canonical spec copy (C13) and mark the other as a pointer. First manual run of the A3 promotion checklist on whatever sits in `06_agent-out` (currently 4 notes).
6. **If validated:** book Sitting 2 for the CN-01 single-ally pilot — the highest-leverage unserved need (`gap-analysis.md` §4.1). Its lane micro-batch is one `12_people`-schema note for the pilot ally.

---

## 6. What we could not verify — questions only you can answer

Each of these is a judgment call where the evidence ran thin. None requires technical knowledge; they are about your intent and experience.

1. **The 2026-06-24/25 migration.** The new lanes are populated, but the spec's authors believed nothing had run. Did you (or a session you ran) intend that migration as done? What stopped the remaining parts — the `12_people` split, the media archival — a decision, or an interruption you'd like picked back up?
2. **The nine-system carve-up.** Does dividing your work and life into these nine named systems match how you actually think about them? If you were carving it yourself, what would you merge, split, or rename?
3. **Gate strictness.** Which kinds of AI output would you happily approve in batches or spot-check (tracker rows, reference summaries, regenerated digests) — and which must you always read line-by-line (anything in your voice? anything about people)?
4. **Cross-system sharing.** Would you be comfortable letting agents read a *small set of structured figures* from each other's approved output — e.g., a runway number or a load limit — without your per-item review? Or does any AI-reads-AI feel like the failure you built the firewall against?
5. **Weekly sittings.** Has a fixed weekly review ever stuck for you for more than a month? What actually gets you to review — a calendar slot, or opening a project you currently care about?
6. **The Cubi's role.** Do you picture the Cubi writing directly into your vault, or handing you results some other way (e.g., your phone reading them straight off the Cubi)? Have you ever seen Obsidian Sync and pCloud fight over the same file?
7. **Backup for the most sensitive material.** Which do you trust more: an encrypted archive on an external disk you keep at home, or an encrypted blob on pCloud? (Both keep the content unreadable to any cloud service.)
8. **Digest cadence.** Given your sleep pattern, would a daily digest actually get read daily — or would it pile up as a small guilt debt? Is weekly, or purely on-demand, the honest answer?
9. **The two spec copies.** Which copy — the audit-workspace one or the one inside your vault — reflects what you currently believe? (We recommend keeping exactly one; you pick which.)
10. **"T3."** When you see "T3," do you read your own meaning (delegation: AI executes, you approve)? Are you fine renaming the privacy scale to P0–P3 so the two never collide?

---

## 7. Appendix — full findings by lens, with verification notes

All 26 findings survived adversarial re-verification; where verification weakened a claim, that is noted. "Verify:" lines report what the checking pass found.

### Lens 1 — Fidelity to Adler's own design (10 findings)

*Lens summary: roughly one-third faithful restatement, one-third plausible extension, one-third invention resting on two false facts (the Stage-B stall claim; the existing-hooks claim). The faithful parts trace to `vault-map.md`/`conventions.md`.*

- **L1-1 (CRIT) — "STALLED at Stage B / staged but not executed" is false.** Spec L51-53 builds its causal story and rollout advice on it. Live vault (checked 2026-07-07): `01_journal` 871, `02_tasks` 1052, `03_projects` 28, `04_ideas` 40 files, populated 2026-06-24/25; source folders gone; open: `12_people` (1 vs 155 in `801`), `07` (0 ever), media (5 vs ~2,570), `10/11` never created, enforcement never built. Fix → C8. *Verify: kept; re-verified counts; migration is even more partial than first stated (`10/11` missing); the aside that `gap-analysis.md` never read the live vault was mildly overstated.*
- **L1-2 (CRIT) — "Enforced by PreToolUse hooks + write-allowlists" is false as current fact.** `C:\Users\adler-standard\.claude\settings.json` has only PreCompact/SessionEnd/Stop hooks; no project settings anywhere. Wording copied from `vault-map.md` L32 (faithful intent, false status). Fix → C3. *Verify: kept; checked all plausible settings locations.*
- **L1-3 (MAJ) — "Emergent cross-system flows" violate the inherited firewall or reinstate the human bottleneck.** Spec L18-22 vs `vault-map.md` L29-32, `conventions.md` L42; never reconciled with L6's bottleneck-removal claim. Fix → C5. *Verify: kept; dilemma follows directly from the cited texts; no Adler-authored doc describes AI-to-AI flows.*
- **L1-4 (MAJ) — Firewall hardened from case-by-case to permanent categorical doctrine.** Spec L15/L27 cite clm-0027 as settled; the actual verdict is "Nuance: depends on use-case" (questionnaire L105), and clm-0039 (L111) frames the gate as provisional and quality-driven. Fix → C4. *Verify: kept; verdicts quoted correctly.*
- **L1-5 (MAJ) — `12_people` misclassified as plain AI-readable canon; canon row also omits `00/02/03/04`.** Spec L47 vs `vault-map.md` L16, `conventions.md` L30-33. Fix → C2. *Verify: kept; ops `CLAUDE.md` corroborates (12_people excluded from the agent's canon read list).*
- **L1-6 (MAJ) — The two live copies contradict each other on the key recommendation and the lane count.** Blueprint L53 "foundational track... in parallel" vs in-vault L43 "not prerequisites... Build ON, never wait"; "15-lane" vs "13-lane". Fix → C13, C14, D1. *Verify: kept verbatim; weakened one citation — clm-0035's verdict is "Nuance: closer to rival" (deliberate deprioritization), so cite as "undeadlined work reliably doesn't get done," not a confirmed stall mechanism.*
- **L1-7 (MAJ) — T3 namespace collision.** Spec L13/L25 "T3 → Cubi-local" vs `conventions.md` L45-47 where T3 = default delegation tier; privacy-T3 exists only in the audit workspace's `00_admin\privacy-tiers.md`. Fix → C10. *Verify: kept; small overstatement in "never disambiguates" (spec L27 parenthetically ties T3 to privacy tiers), but L13/L25 use bare "T3".*
- **L1-8 (min) — Dispatch layer (daily meta-agent, event hooks) is invention beyond anything Adler specified.** His docs contain only `route:`/`deleg:` fields, token-order routing, inbox agent-triage, manual `/reflect`. Spec is honest it's new (L33). Lane usage: `05` 3 files, `06` 4, `07` 0. Fix → C9, D2 (triage-only meta-agent is grounded; event-fired execution is not). *Verify: kept; counts exact.*
- **L1-9 (min) — Blackboard/stigmergy/nine-systems is the spec's vocabulary, not Adler's — but structurally consistent with his design.** clm-0034 (prosthetic architecture, essentially confirmed) and clm-0036 (hub-identity denied) support the structural framing. Fix → C1 (relabel; ratify). *Verify: kept; vocabulary absent from all Adler-authored docs; L38 does claim "the mapping is exact".*
- **L1-10 (min) — "Weekly sitting" invents a cadence over a lane never used.** `07` has 0 files; `skill-map.md` L19 calls periodic reviews lapsed; clm-0031: instance-based reflection; spec's own L56 cap partially mitigates. Fix → C15. *Verify: kept; `_Home.md` already carries a guardrail block, supporting the instance-attached alternative.*

### Lens 2 — Fidelity to Adler's endorsed references (8 findings)

*Lens summary: faithful to his governance canon (human-gated AI writes, GTD rhythm, no PARA) — unfaithful to his thinking canon (Zettelkasten/LYT linking layer entirely absent); the stigmergy headline contradicts the firewall it enforces.*

- **L2-1 (MAJ) — Claims to be the everyday note-taking system while omitting the entire linking/thinking layer.** No wikilinks/MOCs/Dataview/graph/block-linking/`04_ideas` row anywhere in the spec, vs `conventions.md` L56-57, `me.md` L10/L31/L46, `vault-map.md` L10, `metaref1` L13-16, and the 2021 LYT webclip showing the philosophy predates adlerOS. Fix → A4. *Verify: kept; one overstatement — spec L42's dashboard row does mention "ideas-to-publish"; the linking-layer omission stands.*
- **L2-2 (MAJ) — Stigmergy sold without disclosing that all cross-system throughput funnels through the one human gate.** Same dilemma as L1-3 from the reference side; the firewall itself is the hardened form of the human-in-the-loop caveat in the Karpathy material he engaged with (`conversation-history-raw.md` ~L1293). Fix → C5. *Verify: kept; quote located, though it sits in an AI research report (Turn 66) — "adopted deliberately" is inference from his own firewall docs.*
- **L2-3 (min) — Canon row misstates lanes; spec's own layers read lanes its canon row excludes.** Dispatch reads `00_horizons` (L12-13, L33); blackboard includes `02/03` (L25). Fix → C2. *Verify: exact.*
- **L2-4 (min) — `07` as dispatch work-queue strains the lane's quarantine semantics.** A queue must be re-read; `vault-map.md` L13 says never fed back; `06` is the designed lane for transient proposals. Fix → C7. *Verify: kept; read-back dilemma is fair inference.*
- **L2-5 (min) — Daily re-synthesis is in tension with his anti-rag "crystal" stance.** `anti-rag.md` L7-19, L100-104, L215-219: freeze, flag staleness, regenerate deliberately; the spec's digests/routing have no frozen form. Fix → A6. *Verify: kept; all lines land, including the daily-brief request in `metaref1` L17.*
- **L2-6 (min) — Lanes do NOT violate LYT (provenance/write-permission, not topic taxonomy) — but the spec never says so.** `vault-map.md` L37, `conventions.md` L57, `me.md` L31. Fix → A5. *Verify: kept.*
- **L2-7 (min) — Reference-mandated "manual fallback for every automated step" is missing for the firewall itself.** `metaref1` L153-158; hooks unbuilt (L1-2); no interim manual protocol in the spec. Fix → A3. *Verify: kept; exact quotes located.*
- **L2-8 (min) — Positive: governance loop is near-verbatim his adopted governance model; GTD rhythm preserved; PARA correctly absent.** `metaref1` §3.4 L470-483 → `conventions.md` L39-43; `skill-map.md` L5-19. Fix → K3 (treat as stable during revision). *Verify: kept; only the aside about an explicit "avoid PARA" instruction couldn't be traced to a cited line.*

### Lens 3 — Technical soundness (10 findings)

*Lens summary: right architecture in kind (blackboard over vault folders; nothing needs point-to-point calls), unbuildable as written in five places (Cubi write path over double-sync; overclaimed enforcement; quarantine rule vs the design's own loops; event hooks; T3 with no backup). Every gap has a cheap fix.*

- **L3-1 (CRIT) — Double-sync topology with an unspecified Cubi write path; phone-digest chain silently depends on the laptop being awake with Obsidian open.** `migration-runbook.md` L3, 8-12, 62; memory note on Obsidian Sync; Cubi bootstrap includes no Obsidian/pCloud client. Fix → A1. *Verify: kept; one caveat — the blueprint also gives the phone an SSH-to-Cubi path, so the vault chain is one of two spec'd read paths (the recommendation already uses the other).*
- **L3-2 (CRIT) — Hook firewall is enforceable only write-side, only inside Claude Code, only via folder paths; spec oversells it as a general guarantee.** Four bypass routes: shell writes; dual path roots (C: and P:); non-Claude-Code runtimes (Ollama/Open WebUI); frontmatter promotion invisible to path rules. Fix → C3 (folder-as-enforcement-unit; promotion = move; honest scope statement). *Verify: kept; one trivial mis-attribution (Ollama cite belongs to `conventions.md` L51).*
- **L3-3 (CRIT) — "T3 outside every sync path" deletes durability; the most sensitive artifacts have zero backup.** Spec L25; `capacital-blueprint.md` §3.7-3.9, §6; contrast three vault backups in `migration-runbook.md` L3-4, 67-68. Fix → C12. *Verify: kept; asymmetry exactly as claimed.*
- **L3-4 (MAJ) — The literal "never read back" rule forbids the digest/tracker loops the design depends on.** Tracker in `06` re-read across runs (`capacital-blueprint.md` §4); status-digest reads the tracker (`operations-coordination.md` L79). Fix → C6. *Verify: kept; one loose example (the meta-agent reads shared/canon lanes, not AI lanes) — the tracker loops alone establish the contradiction.*
- **L3-5 (MAJ) — No item-claim protocol; three dispatch triggers can process the same item; concurrent tracker writes lose updates silently.** `operations-coordination.md` L83-91, 110-113; `migration-runbook.md` L62. Fix → A2. *Verify: kept; no claim/processed marker exists in any schema.*
- **L3-6 (MAJ) — Privacy-routing bootstrap hole: classifying a capture requires reading it; a cloud read of a sensitive capture is already the leak.** Spec L13/L26; `capacital-blueprint.md` §6; a haiku-tier (cloud) extractor sits directly on raw inbox capture in `operations-coordination.md` L67. Fix → C11. *Verify: kept; the "health/family material in the inbox" premise is flagged inference from the inbox's catch-all role, not a citation.*
- **L3-7 (MAJ) — "Event hooks" are not buildable with Claude Code hooks; they re-specify the dead capture daemon.** Spec L26; `operations-coordination.md` L81-85; `gap-analysis.md` row 4. Fix → D2. *Verify: kept; the blueprint's own hook definition repeats the conflation.*
- **L3-8 (MAJ) — Internal contradiction: "unstall Stage B" foundational track vs the same file's "not a big-bang build".** Spec L29-35 vs L51-53; `gap-analysis.md` rows 1/15, §3 item 4. Fix → D1. *Verify: kept; "contradiction" slightly strong (L53 concedes a legacy-lane start; it is a half-acknowledged tension), and one internal cross-reference ("binding constraint 8") had no clean referent.*
- **L3-9 (MAJ) — Daily meta-agent: premature single point of failure, silent by design, on an unvalidated local model.** Pull-only interface means no failure signal; Qwythos benchmark is speed-only (`HANDOFF.md` L13). Fix → C9. *Verify: kept; minor nuance — the weekly digest may run on laptop too; all standing schedules are Cubi-cron.*
- **L3-10 (min) — Five components deletable at MVP with <10% value loss:** meta-agent (→ manual skill + weekly cron), event hooks (→ polling), cross-system load-governor wiring (→ one prompt line reading `00_horizons`), AI-wiki (self-declared regenerable), System-1 voice calibration (a CN-03 dependency smuggled into the CN-01 pilot, whose own MVP already excludes agents). Fix → D3. *Verify: kept; the <10% figure is judgment; every factual citation checks out.*

### Lens 4 — Adoption / stall risk (8 findings)

*Lens summary: half-saved by its own "NOT a big-bang build" section; four components are platform-before-it-pays (daily meta-agent, hook machine, Cubi lane-analog, event hooks); the "parallel foundational track" is the documented failure mode wearing a new name.*

- **L4-1 (min) — The "grow it, don't build it" core is adoption-realistic; keep it as the spine.** Spec L29-35, L56 match the stall diagnostic (`gap-analysis.md` §2) and the finished-systems pattern (narrow + deadlined ships). Minimum-viable System 0 = live `05/06/07` lanes + router doc + conventions page + manual invocation + human promotion. Fix → K6. *Verify: kept; one trivial attribution slip (component-15 evidence is in `gap-analysis.md`).*
- **L4-2 (CRIT) — Four components are platform-before-it-pays with highest abandonment risk:** daily meta-agent, hook-enforcement machine, Cubi T3 lane-analog, event-hook dispatch — all standing infrastructure, no deadline, no bread anchor (clm-0035/0010; `gap-analysis.md` §2, L17). Fix → C9, C3 (demand-driven trigger), D3, D2. *Verify: kept; the "daily review load" point is interpretation; every evidentiary citation checks out.*
- **L4-3 (MAJ) — Firewall as absolute law exceeds what Adler endorsed and adds adoption friction.** Same evidence as L1-4 plus `gap-analysis.md` §3.8's genuinely non-negotiable subset (no automated relational delivery; no health-verdict automation). Fix → C4. *Verify: kept; the spec really does cite clm-0027 as warrant for more than the verdict says.*
- **L4-4 (MAJ) — Lanes-work-today (L31) vs power-depends-on-finishing-Stage-B (L53): a finish-first prerequisite re-created.** `gap-analysis.md` L68 states the opposite; systems feel uncoordinated because zero of nine exist (§2 tally). Fix → C8, D1. *Verify: kept verbatim.*
- **L4-5 (MAJ) — Why Stage B stalled: per-batch approval cost with no per-batch payoff, single-writer windows (Obsidian closed), no forcing function, no consumer — and partially Cubi-gating.** `migration-runbook.md` L48-49, 62, 68; the design itself is sound (additive, rollback-safe); the stall is motivational-structural. Fix → D1 (pull-based batches; laptop-only; staged conventions as first additive batch). *Verify: kept, weakened on one cause — the "once the Cubi is up" gate applies to the delegated-jobs queue, not the lane moves; causes 2-4 fully check out.*
- **L4-6 (CRIT) — Q2 verdict: scorer START-HERE; dissolve (don't parallel) Stage B.** Full reasoning in §5. *Verify: kept, weakened on framing — the corpus's Q2 offers only two options (lead-with-Stage-B vs scorer+parallel-track); the "strictly sequential" third option was the lens's own construction. `capacital-blueprint.md` L168's sentence about how the 13-lane vault and capture daemon died is verbatim-real.*
- **L4-7 (min) — Daily cadence conflicts with the sleep-phase pattern and the health-based schedule governor.** clm-0024 (questionnaire L95), clm-0023 (L94), `persona-v3.1.md` §3; the blueprint's own "pull, not push" language (L152) never made it into the System 0 spec. Fix → C9. *Verify: kept; all citations land.*
- **L4-8 (min) — Lane-count inconsistency (13 vs 15) across the audit's own documents.** `vault-map.md` = 15; spec L51 and `HANDOFF.md` L23 = 15; `gap-analysis.md`/`capacital-blueprint.md` (and the in-vault spec copy L43) = 13. Fix → C14. *Verify: kept, but the drift points the other way than first claimed — the spec's 15 MATCHES ground truth; the stale 13 lives in the earlier audit docs.*

---

*Report author: System 0 re-audit synthesis, 2026-07-07. All evidence local. Third parties role-tagged; no verbatim personal material quoted.*

---

## Addendum — post-audit corrections (2026-07-07, evening)

Three facts learned after the report was written; they correct or sharpen findings above.

1. **Lanes 10/11 exist on disk** under different names than `vault-map.md` uses: `10_commonknowledge` and `11_myconcepts` (vs the map's `10_knowledge`/`11_frameworks`). L1-1/C8's "never created" is a naming mismatch, not an absence. Adds one small item to the pull-based migration list: reconcile the names (or update vault-map).
2. **The Cubi DOES have a vault client — A1/L3-1 overstated the gap.** A nightly cron (03:00) on the Cubi runs `ob sync` (npm CLI speaking Obsidian Sync directly — no laptop involvement) into `~/vault`, then commits and pushes to a private GitHub archive (`adler22221/adleros-vault`). Verified working (last run 2026-07-06, committed+pushed). So: the Cubi has a read path to the vault AND the vault has a fourth backup (git history). Still true and still needed from A1: the *write* path and single-writer rules are unspecified — the cron is read/archive-only today.
3. **The Qwythos-9B speed verdict is real and reproducible.** Live benchmark 2026-07-07 inside the Docker ollama container (`hf.co/empero-ai/Qwythos-9B-Claude-Mythos-5-1M-GGUF:Q4_K_M`, 6.8 GB): eval 3.75 tok/s, prompt-eval 11.68 tok/s, no swap, no container limits, RAM ample (13 GB free). The bottleneck is physics, not configuration: CPU token generation streams all ~6.8 GB of weights per token through a 15 W laptop-class chip (i7-1255U, 2P+8E cores), so ~4-6 tok/s is the hardware's ceiling; and this model is a *thinking* model (generated 411 tokens for a 100-word answer), which multiplies wall time ~3x. RAM size was never the limiter — reviews saying "8 GB suffices" address whether a model *fits*, not how fast it runs. Practical levers, in order: (a) pull a 3-4B non-thinking instruct model for routine triage/digest work (~2-3x faster); (b) disable thinking for routine tasks on the 9B; (c) check whether the Cubi has one or two RAM sticks (single-channel would halve bandwidth; a second stick ≈ doubles speed) — needs sudo/BIOS; (d) keep the Cubi's role as overnight queued work (at 3.75 tok/s it still produces >100k tokens per night), never interactive or bulk.

## Addendum 2 — Adler's reprioritization (2026-07-07, evening; supersedes §5's Week-1 ordering)

Adler's own priority call, verbatim in intent: the vault is under construction and unusable day-to-day ("I don't know where and how to add notes"; no template auto-population); **first make adlerOS work again for everyday functioning**; and **build the phone→adlerOS capture pipeline** (Claude Code, Hermes agent, or anything) — the architecture is paper-only until that exists. The opportunity-fit-scorer moves behind these. This is consistent with the audit's own findings (A4: the thinking layer is the missing half; L4-1: minimum-viable System 0 = live lanes + conventions + manual invocation) — the audit simply ranked a different entry point, and the owner overruled it.
